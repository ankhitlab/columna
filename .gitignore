# Dependencies
node_modules

# Build outputs
dist
*.tsbuildinfo
target
pkg
pkg-bundler
*.node

# Test / coverage
coverage

# Caches
.turbo
.vite
.cache
*.log

# OS / IDE
.DS_Store
Thumbs.db
.idea
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example

# Env / secrets
.env
.env.*
!.env.example
*.pem
*.key
credentials.json

# Benchmark result dumps (keep .gitkeep)
packages/bench/results/*.json
packages/bench/results/*.csv
packages/bench/results/*.log
!packages/bench/results/.gitkeep

# Regenerable / scale-specific bench reports (canonical public report is docs/comparison-js.md = 2M)
docs/comparison-js-*m.md
docs/*.md.bak

# Local profiling / one-off microbench harnesses (not part of the public tree)
packages/bench/src/profile-*.mts
packages/bench/src/profile-*.ts
scripts/bench-*.mts
scripts/bench-*.mjs
# scripts/consumer-test.mjs stays tracked — used by CI (`test:consumer`)

# Local npm name-research / claim scratch (not for public repo)
_npm-free.txt
_npm-taken.txt
_npm-name-check.json
_columna-claim/
_dfkit-claim/

# Pack artifacts
*.tgz
packages/browser-smoke/dist

# Local-only IDE (not part of the public repository)
packages/studio/
coverage/
docs/api/
site/
