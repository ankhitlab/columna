{
  "name": "@columna/browser-smoke",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Builds a page against the published columna bundles and runs it in headless Chromium (CPU path always; WebGPU path when the browser exposes it).",
  "scripts": {
    "build": "vite build",
    "test": "node run.mjs",
    "test:ci": "vite build && node run.mjs"
  },
  "dependencies": {
    "columna": "workspace:*"
  },
  "devDependencies": {
    "playwright": "^1.50.0",
    "vite": "^6.0.3"
  }
}
